apiVersion: v1
kind: Service
metadata:
 labels:
   app: udagram-reverse-proxy
 name: udagram-reverse-proxy
spec:
 ports:
 - name: http
   port: 8080
   targetPort: 8080
 selector:
   app: udagram-reverse-proxy
 type: LoadBalancer